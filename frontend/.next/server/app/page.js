(() => {
var exports = {};
exports.id = 931;
exports.ids = [931];
exports.modules = {

/***/ 75193:
/***/ ((module) => {

"use strict";
module.exports = require("@emotion/react/jsx-runtime");

/***/ }),

/***/ 18038:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/compiled/react");

/***/ }),

/***/ 98704:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/compiled/react-dom/server-rendering-stub");

/***/ }),

/***/ 97897:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/compiled/react-server-dom-webpack/client");

/***/ }),

/***/ 56786:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/compiled/react/jsx-runtime");

/***/ }),

/***/ 5868:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/server/app-render/app-render");

/***/ }),

/***/ 41844:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/server/app-render/get-segment-param");

/***/ }),

/***/ 96624:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/server/future/helpers/interception-routes");

/***/ }),

/***/ 75281:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/server/future/route-modules/route-module");

/***/ }),

/***/ 57085:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/app-router-context");

/***/ }),

/***/ 20199:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/hash");

/***/ }),

/***/ 39569:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/hooks-client-context");

/***/ }),

/***/ 17160:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router-context");

/***/ }),

/***/ 30893:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/add-path-prefix");

/***/ }),

/***/ 12336:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/format-url");

/***/ }),

/***/ 17887:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/handle-smooth-scroll");

/***/ }),

/***/ 98735:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/is-bot");

/***/ }),

/***/ 60120:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/is-local-url");

/***/ }),

/***/ 68231:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/parse-path");

/***/ }),

/***/ 54614:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/path-has-prefix");

/***/ }),

/***/ 53750:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/remove-trailing-slash");

/***/ }),

/***/ 70982:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/resolve-href");

/***/ }),

/***/ 79618:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/server-inserted-html");

/***/ }),

/***/ 78423:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/utils");

/***/ }),

/***/ 71017:
/***/ ((module) => {

"use strict";
module.exports = require("path");

/***/ }),

/***/ 57310:
/***/ ((module) => {

"use strict";
module.exports = require("url");

/***/ }),

/***/ 56274:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   GlobalError: () => (/* reexport default from dynamic */ next_dist_client_components_error_boundary__WEBPACK_IMPORTED_MODULE_2___default.a),
/* harmony export */   __next_app__: () => (/* binding */ __next_app__),
/* harmony export */   originalPathname: () => (/* binding */ originalPathname),
/* harmony export */   pages: () => (/* binding */ pages),
/* harmony export */   routeModule: () => (/* binding */ routeModule),
/* harmony export */   tree: () => (/* binding */ tree)
/* harmony export */ });
/* harmony import */ var next_dist_server_future_route_modules_app_page_module__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(7262);
/* harmony import */ var next_dist_server_future_route_modules_app_page_module__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(next_dist_server_future_route_modules_app_page_module__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var next_dist_server_future_route_kind__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(19513);
/* harmony import */ var next_dist_client_components_error_boundary__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(31823);
/* harmony import */ var next_dist_client_components_error_boundary__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(next_dist_client_components_error_boundary__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(12502);
/* harmony import */ var next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_3__);
/* harmony reexport (unknown) */ var __WEBPACK_REEXPORT_OBJECT__ = {};
/* harmony reexport (unknown) */ for(const __WEBPACK_IMPORT_KEY__ in next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_3__) if(["default","tree","pages","GlobalError","originalPathname","__next_app__","routeModule"].indexOf(__WEBPACK_IMPORT_KEY__) < 0) __WEBPACK_REEXPORT_OBJECT__[__WEBPACK_IMPORT_KEY__] = () => next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_3__[__WEBPACK_IMPORT_KEY__]
/* harmony reexport (unknown) */ __webpack_require__.d(__webpack_exports__, __WEBPACK_REEXPORT_OBJECT__);
// @ts-ignore this need to be imported from next/dist to be external


const AppPageRouteModule = next_dist_server_future_route_modules_app_page_module__WEBPACK_IMPORTED_MODULE_0__.AppPageRouteModule;
// We inject the tree and pages here so that we can use them in the route
// module.
const tree = {
        children: [
        '',
        {
        children: ['__PAGE__', {}, {
          page: [() => Promise.resolve(/* import() eager */).then(__webpack_require__.bind(__webpack_require__, 92838)), "/Users/yum.ishii/SaraokuDB/frontend/app/page.tsx"],
          metadata: {
    icon: [(async (props) => (await Promise.resolve(/* import() eager */).then(__webpack_require__.bind(__webpack_require__, 57481))).default(props))],
    apple: [],
    openGraph: [],
    twitter: [],
    manifest: undefined
  }
        }]
      },
        {
        'layout': [() => Promise.resolve(/* import() eager */).then(__webpack_require__.bind(__webpack_require__, 51921)), "/Users/yum.ishii/SaraokuDB/frontend/app/layout.tsx"],
'loading': [() => Promise.resolve(/* import() eager */).then(__webpack_require__.bind(__webpack_require__, 96330)), "/Users/yum.ishii/SaraokuDB/frontend/app/loading.tsx"],
'not-found': [() => Promise.resolve(/* import() eager */).then(__webpack_require__.t.bind(__webpack_require__, 95493, 23)), "next/dist/client/components/not-found-error"],
        metadata: {
    icon: [(async (props) => (await Promise.resolve(/* import() eager */).then(__webpack_require__.bind(__webpack_require__, 57481))).default(props))],
    apple: [],
    openGraph: [],
    twitter: [],
    manifest: undefined
  }
      }
      ]
      }.children;
const pages = ["/Users/yum.ishii/SaraokuDB/frontend/app/page.tsx"];

// @ts-expect-error - replaced by webpack/turbopack loader

const __next_app_require__ = __webpack_require__
const __next_app_load_chunk__ = () => Promise.resolve()
const originalPathname = "/page";
const __next_app__ = {
    require: __next_app_require__,
    loadChunk: __next_app_load_chunk__
};

// Create and export the route module that will be consumed.
const routeModule = new AppPageRouteModule({
    definition: {
        kind: next_dist_server_future_route_kind__WEBPACK_IMPORTED_MODULE_1__.RouteKind.APP_PAGE,
        page: "/page",
        pathname: "/",
        // The following aren't used in production.
        bundlePath: "",
        filename: "",
        appPaths: []
    },
    userland: {
        loaderTree: tree
    }
});

//# sourceMappingURL=app-page.js.map

/***/ }),

/***/ 81588:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

Promise.resolve(/* import() eager */).then(__webpack_require__.bind(__webpack_require__, 42185))

/***/ }),

/***/ 42185:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  "default": () => (/* binding */ Home)
});

// EXTERNAL MODULE: ./node_modules/@emotion/react/jsx-runtime/dist/emotion-react-jsx-runtime.esm.js
var emotion_react_jsx_runtime_esm = __webpack_require__(75284);
// EXTERNAL MODULE: ./src/components/ui/button.tsx
var ui_button = __webpack_require__(29256);
// EXTERNAL MODULE: ./node_modules/next/link.js
var next_link = __webpack_require__(11440);
var link_default = /*#__PURE__*/__webpack_require__.n(next_link);
// EXTERNAL MODULE: ./node_modules/next/navigation.js
var navigation = __webpack_require__(57114);
// EXTERNAL MODULE: external "next/dist/compiled/react"
var react_ = __webpack_require__(18038);
;// CONCATENATED MODULE: ./src/utils/price.ts
// 数値に変換するユーティリティ関数
function toNumber(val) {
    const n = Number(val);
    return Number.isFinite(n) ? n : null;
}
// 有効な正の数値かどうかをチェックする関数
function isValidPositive(n) {
    if (Array.isArray(n) && n.length > 0) {
        // 配列の場合は最後の要素をチェック
        const last = n[n.length - 1];
        const num = Number(last);
        return Number.isFinite(num) && num > 0;
    }
    const num = Number(n);
    return Number.isFinite(num) && num > 0;
}
// 価格を表示用の文字列に変換する関数
function getDisplayPrice(horse) {
    if (!horse) return "価格未設定";
    // 主取りの判定
    const isUnsold = horse.unsold === true || horse.is_unsold === true || typeof horse.is_unsold === "string" && horse.is_unsold.toLowerCase() === "true" || horse.sold_price === null || horse.sold_price === undefined || horse.sold_price === "[null]" || horse.sold_price === "null";
    if (isUnsold) {
        return "主取り";
    }
    // 配列の場合の処理（例: [1020000] のような形式）
    if (Array.isArray(horse.sold_price)) {
        // 配列内の最後の有効な価格を取得
        const lastValidPrice = horse.sold_price.map((price)=>{
            // 文字列の場合は数値に変換を試みる
            if (typeof price === "string") {
                // 角括弧で囲まれた文字列を処理（例: "[1020000]"）
                const cleanPrice = price.replace(/[\[\]"\s]/g, "");
                const num = Number(cleanPrice);
                return isNaN(num) ? null : num;
            }
            return Number(price);
        }).filter((price)=>price !== null && !isNaN(price) && price > 0).pop();
        if (lastValidPrice !== undefined) {
            return `¥${lastValidPrice.toLocaleString()}`;
        }
        return "主取り";
    }
    // 文字列の価格の場合（例: "310000" または "[310000]"）
    if (typeof horse.sold_price === "string") {
        // 角括弧を削除して数値に変換
        const cleanPrice = horse.sold_price.replace(/[\[\]]/g, "");
        const price = Number(cleanPrice);
        if (!isNaN(price) && price > 0) {
            // 3桁区切りの数値にフォーマット（例: 310000 → 310,000）
            return `¥${price.toLocaleString()}`;
        }
        return "主取り";
    }
    // 数値の価格の場合
    if (typeof horse.sold_price === "number") {
        return `¥${horse.sold_price.toLocaleString()}`;
    }
    // 履歴から価格を取得
    if (horse.history && horse.history.length > 0) {
        const latestHistory = horse.history[0];
        if (latestHistory) {
            // 履歴内の主取りフラグをチェック
            const isHistoryUnsold = latestHistory.unsold === true || latestHistory.is_unsold === true || typeof latestHistory.is_unsold === "string" && latestHistory.is_unsold.toLowerCase() === "true";
            if (isHistoryUnsold) {
                return "主取り";
            }
            // 履歴内の価格をチェック
            if (latestHistory.sold_price) {
                if (Array.isArray(latestHistory.sold_price)) {
                    const validPrices = latestHistory.sold_price.map((price)=>Number(price)).filter((price)=>!isNaN(price) && price > 0);
                    if (validPrices.length > 0) {
                        return `¥${validPrices[validPrices.length - 1].toLocaleString()}`;
                    }
                } else if (typeof latestHistory.sold_price === "string") {
                    const price = Number(latestHistory.sold_price.replace(/[^0-9.-]+/g, ""));
                    if (!isNaN(price) && price > 0) {
                        return `¥${price.toLocaleString()}`;
                    }
                } else if (typeof latestHistory.sold_price === "number" && latestHistory.sold_price > 0) {
                    return `¥${latestHistory.sold_price.toLocaleString()}`;
                }
            }
        }
    }
    // トップレベルのpriceをチェック
    if (horse.price) {
        if (Array.isArray(horse.price)) {
            const validPrices = horse.price.map((price)=>Number(price)).filter((price)=>!isNaN(price) && price > 0);
            if (validPrices.length > 0) {
                return `¥${validPrices[validPrices.length - 1].toLocaleString()}`;
            }
        } else if (typeof horse.price === "string") {
            const price = Number(horse.price.replace(/[^0-9.-]+/g, ""));
            if (!isNaN(price) && price > 0) {
                return `¥${price.toLocaleString()}`;
            }
        } else if (typeof horse.price === "number" && horse.price > 0) {
            return `¥${horse.price.toLocaleString()}`;
        }
    }
    // デフォルト
    return "価格未設定";
}
function formatPrice(val) {
    // 円を前提にローカライズ
    return `¥${Number(val).toLocaleString("ja-JP")}`;
}

// EXTERNAL MODULE: ./src/utils/format.ts
var format = __webpack_require__(27686);
// EXTERNAL MODULE: ./node_modules/react-icons/fa/index.esm.js
var index_esm = __webpack_require__(43500);
// EXTERNAL MODULE: ./src/utils/url.ts
var url = __webpack_require__(97084);
;// CONCATENATED MODULE: ./src/components/AnalysisContent.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 






// 馬体重をフォーマットする関数（整数値のみを想定）
function formatWeight(weight) {
    console.log("formatWeight called with:", weight, "type:", typeof weight);
    if (weight === null || weight === undefined || weight === "") {
        return "-";
    }
    // 数値に変換して整数に丸める
    const num = typeof weight === "string" ? parseInt(weight, 10) : Math.floor(Number(weight));
    console.log("formatted weight:", num);
    return isNaN(num) ? "-" : `${num}kg`;
}


// 馬データを変換する関数
const transformHorseData = (data)=>{
    console.log("=== transformHorseData called ===");
    console.log("Input data:", data);
    // データ構造の検証
    if (!data || typeof data !== "object") {
        console.error("Invalid data format: Expected an object", data);
        return [];
    }
    // 馬データの配列を取得
    const horsesArray = Array.isArray(data) ? data : data.horses && Array.isArray(data.horses) ? data.horses : [];
    // メタデータを保持
    const metadata = data.metadata || {};
    console.log(`Transforming ${horsesArray.length} horses`);
    if (horsesArray.length > 0) {
        console.log("First horse keys:", Object.keys(horsesArray[0]));
        console.log("First horse data:", JSON.stringify(horsesArray[0], null, 2));
    }
    try {
        console.log("Starting to process horses...");
        const filteredHorses = horsesArray.filter((horse)=>{
            if (!horse) {
                console.log("Found null or undefined horse, filtering out");
                return false;
            }
            console.log(`Processing horse: ${horse.name || "No name"} (ID: ${horse.id || "No ID"})`);
            if (!horse.id && !horse.name) {
                console.log("Horse missing required fields (id and name):", JSON.stringify(horse, null, 2));
                return false;
            }
            return true;
        });
        console.log(`Filtered ${horsesArray.length - filteredHorses.length} invalid horses`);
        const result = filteredHorses.map((horse, index)=>{
            console.log(`[${index}] Mapping horse:`, horse.name || "No name");
            // IDを明示的に文字列に変換
            const horseId = horse.id ? String(horse.id) : `horse-${Date.now()}`;
            // オークション履歴を取得（historyまたはauction_historyのいずれかを使用）
            const auctionHistory = Array.isArray(horse.history) ? horse.history : Array.isArray(horse.auction_history) ? horse.auction_history : [];
            // 馬体重を取得するヘルパー関数
            const getEffectiveWeight = ()=>{
                // 1. 馬オブジェクトの体重を確認
                if (horse.weight !== undefined && horse.weight !== null && horse.weight !== "") {
                    // 数値に変換してチェック
                    const weight = Number(horse.weight);
                    if (!isNaN(weight) && weight > 0) {
                        console.log(`[${index}] ${horse.name} - Using horse.weight:`, weight);
                        return Math.floor(weight); // 整数に丸める
                    }
                }
                // 2. オークション履歴から最新の体重を確認
                if (auctionHistory.length > 0) {
                    // 日付でソート（最新が先頭）
                    const sortedAuctions = [
                        ...auctionHistory
                    ].sort((a, b)=>new Date(b.auction_date).getTime() - new Date(a.auction_date).getTime());
                    // 有効な体重を持つ最初のオークションを探す
                    const validAuction = sortedAuctions.find((auction)=>{
                        if (auction.weight !== undefined && auction.weight !== null && auction.weight !== "") {
                            const weight = Number(auction.weight);
                            return !isNaN(weight) && weight > 0;
                        }
                        return false;
                    });
                    if (validAuction) {
                        const weight = Number(validAuction.weight);
                        console.log(`[${index}] ${horse.name} - Using auction weight (${validAuction.auction_date}):`, weight);
                        return weight;
                    }
                }
                console.log(`[${index}] ${horse.name} - No valid weight found`);
                return null;
            };
            const horseWeight = getEffectiveWeight();
            const displayWeight = horseWeight !== null ? `${horseWeight}kg` : "不明";
            console.log(`[${index}] ${horse.name} - Final effective weight:`, horseWeight);
            // デフォルトのオークションデータ
            const defaultAuction = {
                id: horse.auction_id || "",
                horse_id: horseId,
                auction_date: horse.auction_date || "",
                sold_price: horse.sold_price || null,
                total_prize_start: horse.total_prize_start || 0,
                total_prize_latest: horse.total_prize_latest || 0,
                weight: horseWeight,
                seller: "",
                is_unsold: false,
                comment: "",
                created_at: new Date().toISOString(),
                detail_url: "",
                auction_url: ""
            };
            // オークション履歴を日付でソート（最新が先頭に来るように）
            const sortedAuctions = [
                ...auctionHistory
            ].sort((a, b)=>new Date(b.auction_date).getTime() - new Date(a.auction_date).getTime());
            // 最新のオークション情報を取得（なければデフォルト値を使用）
            const latestAuction = sortedAuctions[0] || defaultAuction;
            // デバッグ用ログ
            console.log(`[${index}] ${horse.name} - latestAuction:`, {
                id: latestAuction?.id,
                auction_date: latestAuction?.auction_date,
                sold_price: latestAuction?.sold_price,
                weight: latestAuction?.weight
            });
            // 販売価格を取得（馬オブジェクト直下のsold_priceを優先）
            // 馬オブジェクトにsold_priceがなく、オークション履歴にsold_priceがある場合はそれを使用
            // どちらにも値がない場合はnullを設定
            let soldPrice = null;
            let priceSource = "none";
            // 1. 馬オブジェクトのsold_priceを確認
            if (horse.sold_price !== undefined && horse.sold_price !== null) {
                const price = Number(horse.sold_price);
                if (!isNaN(price)) {
                    soldPrice = price;
                    priceSource = "horse";
                }
            }
            // 2. オークション履歴のsold_priceを確認
            if ((soldPrice === null || soldPrice === 0) && latestAuction.sold_price !== undefined && latestAuction.sold_price !== null) {
                const price = Number(latestAuction.sold_price);
                if (!isNaN(price)) {
                    soldPrice = price;
                    priceSource = "auction";
                }
            }
            console.log(`[${index}] ${horse.name} - soldPrice:`, {
                value: soldPrice,
                source: priceSource,
                horse_sold_price: horse.sold_price,
                auction_sold_price: latestAuction.sold_price
            });
            // 主取りの判定: 
            // 1. 明示的にis_unsoldまたはunsoldフラグが立っている場合
            // 2. オークションが終了していて、sold_priceがnullまたは0の場合
            const auctionEndDate = latestAuction?.auction_date ? new Date(latestAuction.auction_date) : null;
            const isAuctionEnded = auctionEndDate ? auctionEndDate < new Date() : false;
            const hasUnsoldFlag = horse.is_unsold === true || horse.unsold === true || latestAuction && (latestAuction.is_unsold === true || latestAuction.unsold === true);
            const isHorseUnsold = hasUnsoldFlag || isAuctionEnded && (soldPrice === null || soldPrice <= 0);
            console.log(`[${index}] ${horse.name} - isHorseUnsold:`, isHorseUnsold, {
                horse_unsold: horse.unsold,
                horse_is_unsold: horse.is_unsold,
                latestAuction_unsold: latestAuction?.unsold,
                latestAuction_is_unsold: latestAuction?.is_unsold,
                soldPrice,
                priceSource,
                auction_ended: isAuctionEnded,
                auction_end_date: auctionEndDate?.toISOString(),
                hasUnsoldFlag,
                decision: isHorseUnsold ? "unsold" : "sold"
            });
            // 馬体重を適切に取得（馬オブジェクトまたはオークション履歴から）
            const effectiveWeight = (()=>{
                // デバッグ用に値を確認
                console.log(`[${index}] ${horse.name} - Raw weight values:`, {
                    horseWeight: horse.weight,
                    horseWeightType: typeof horse.weight,
                    latestAuctionWeight: latestAuction?.weight,
                    latestAuctionWeightType: typeof latestAuction?.weight
                });
                // 数値に変換するヘルパー関数
                const toNumber = (val)=>{
                    if (val === undefined || val === null) return null;
                    const num = Number(val);
                    return !isNaN(num) ? num : null;
                };
                // 馬オブジェクトの体重を取得
                const horseWeight = toNumber(horse.weight);
                // オークション履歴の体重を取得
                const auctionWeight = latestAuction ? toNumber(latestAuction.weight) : null;
                // 有効な体重を返す（0より大きい値のみ有効）
                const result = horseWeight !== null && horseWeight > 0 ? horseWeight : auctionWeight !== null && auctionWeight > 0 ? auctionWeight : undefined;
                console.log(`[${index}] ${horse.name} - Processed weight:`, {
                    horseWeight,
                    auctionWeight,
                    effectiveWeight: result,
                    type: typeof result
                });
                return result;
            })();
            // 馬の基本情報と最新のオークション情報をマージ
            // 馬オブジェクト直下の情報を優先し、なければオークション履歴の情報を使用
            // デフォルト値で初期化
            const defaultAuctionData = {
                id: horseId,
                horse_id: horseId,
                auction_date: "",
                sold_price: soldPrice,
                total_prize_start: 0,
                total_prize_latest: 0,
                weight: effectiveWeight,
                seller: "",
                is_unsold: isHorseUnsold,
                unsold: isHorseUnsold,
                comment: "",
                created_at: new Date().toISOString(),
                detail_url: horse.auction_url || "",
                auction_url: horse.auction_url || ""
            };
            // オークション履歴の情報をマージ（最新のものから順に上書き）
            const effectiveAuction = {
                ...defaultAuctionData,
                ...latestAuction || {},
                // 馬オブジェクト直下の情報で上書き
                id: latestAuction?.id || horseId,
                horse_id: horseId,
                auction_date: horse.auction_date || latestAuction?.auction_date || "",
                sold_price: soldPrice,
                total_prize_start: horse.total_prize_start || latestAuction?.total_prize_start || 0,
                total_prize_latest: horse.total_prize_latest || latestAuction?.total_prize_latest || 0,
                weight: effectiveWeight,
                seller: horse.seller || latestAuction?.seller || "",
                is_unsold: isHorseUnsold,
                unsold: isHorseUnsold,
                comment: horse.comment || latestAuction?.comment || "",
                created_at: latestAuction?.created_at || new Date().toISOString(),
                detail_url: latestAuction?.detail_url || horse.auction_url || "",
                auction_url: latestAuction?.auction_url || horse.auction_url || ""
            };
            console.log(`[${index}] ${horse.name} - effectiveAuction:`, {
                sold_price: effectiveAuction.sold_price,
                is_unsold: effectiveAuction.is_unsold,
                unsold: effectiveAuction.unsold
            });
            // 主取りの理由を記録
            const unsoldReasons = {
                // 価格関連の理由
                soldPriceNull: soldPrice === null,
                soldPriceZero: soldPrice === 0,
                soldPriceUndefined: soldPrice === undefined,
                // 馬オブジェクトのフラグ
                horseIsUnsold: horse.is_unsold === true,
                horseUnsold: horse.unsold === true,
                // オークション履歴のフラグ
                auctionIsUnsold: latestAuction.is_unsold === true,
                auctionUnsold: latestAuction.unsold === true,
                // オークション状態
                isAuctionEnded,
                auctionDate: latestAuction.auction_date,
                // 販売価格情報
                horseSoldPrice: horse.sold_price,
                auctionSoldPrice: latestAuction.sold_price
            };
            // 主取り判定をより正確に行う
            // 1. 明示的にunsoldフラグが立っていいる場合は主取りと判定
            // 2. 販売価格がnullの場合は主取りと判定
            const isUnsold = horse.is_unsold === true || horse.unsold === true || latestAuction.is_unsold === true || latestAuction.unsold === true || soldPrice === null; // soldPriceが0の場合は主取りと判定しない
            // デバッグ用に主取り理由を記録（最初の5頭のみ）
            if (index < 5) {
                const decisionReasons = [];
                if (horse.is_unsold === true) decisionReasons.push("馬オブジェクトのis_unsoldフラグがtrue");
                if (horse.unsold === true) decisionReasons.push("馬オブジェクトのunsoldフラグがtrue");
                if (latestAuction.is_unsold === true) decisionReasons.push("オークション履歴のis_unsoldフラグがtrue");
                if (latestAuction.unsold === true) decisionReasons.push("オークション履歴のunsoldフラグがtrue");
                if (soldPrice === 0) decisionReasons.push("販売価格が0のため主取りと判定");
                if (soldPrice === null) decisionReasons.push("販売価格が未設定");
                console.groupCollapsed(`[${index}] ${horse.name} - ${isHorseUnsold ? "主取り" : "落札"}`);
                console.log("主取り判定の根拠:", decisionReasons.length > 0 ? decisionReasons.join("、") : "主取りの根拠なし（落札済み）");
                console.log("販売価格情報:", {
                    soldPrice,
                    horseSoldPrice: horse.sold_price,
                    auctionSoldPrice: latestAuction.sold_price,
                    isAuctionEnded,
                    auctionDate: latestAuction.auction_date,
                    unsoldReasons: {
                        soldPriceNull: latestAuction.sold_price === null,
                        soldPriceZero: latestAuction.sold_price === 0,
                        soldPriceUndefined: latestAuction.sold_price === undefined,
                        isUnsoldFlag: latestAuction.is_unsold === true,
                        unsoldFlag: latestAuction.unsold === true
                    }
                });
                console.log("フラグ状態:", {
                    horse: {
                        is_unsold: horse.is_unsold,
                        unsold: horse.unsold
                    },
                    latestAuction: {
                        is_unsold: latestAuction.is_unsold,
                        unsold: latestAuction.unsold
                    }
                });
                console.log("詳細情報:", {
                    horseId: horse.id,
                    name: horse.name,
                    auctionDate: latestAuction.auction_date || horse.auction_date,
                    weight: effectiveWeight !== null ? effectiveWeight : "不明",
                    comment: horse.comment ? horse.comment.length > 50 ? horse.comment.substring(0, 50) + "..." : horse.comment : "コメントなし",
                    isUnsold
                });
                console.groupEnd();
            }
            // 体重を取得（undefinedの場合は0を返す）
            const weight = effectiveWeight !== undefined ? effectiveWeight : 0;
            // 賞金情報を取得（prize_money.total_prize または latestAuction.total_prize_latest から取得）
            let prizeMoney = 0;
            if (horse.prize_money?.total_prize) {
                // 例: "2,000,000円" から数値に変換
                const prizeStr = horse.prize_money.total_prize.replace(/[^0-9]/g, "");
                prizeMoney = parseInt(prizeStr, 10) || 0;
            } else {
                prizeMoney = latestAuction.total_prize_latest ?? 0;
            }
            const numericSoldPrice = typeof soldPrice === "string" ? parseFloat(soldPrice) || 0 : soldPrice || 0;
            const numericPrizeMoney = typeof prizeMoney === "string" ? parseFloat(prizeMoney) || 0 : prizeMoney || 0;
            // ROI計算（落札価格が0の場合は0）
            const roi = numericSoldPrice > 0 ? numericPrizeMoney / numericSoldPrice * 100 : 0;
            // 画像URLの正規化
            let imageUrl = "";
            if (typeof horse.image_url === "object" && horse.image_url !== null) {
                imageUrl = horse.image_url.image_url || "";
            } else if (typeof horse.image_url === "string") {
                imageUrl = horse.image_url;
            }
            const imageUrlNormalized = (0,url/* normalizeImageUrl */.V)("http://localhost:8001" || 0, imageUrl);
            // 基本情報を設定
            const baseHorse = {
                id: horseId,
                name: horse.name || "不明",
                age: typeof horse.age === "number" ? horse.age : parseInt(horse.age) || 0,
                sex: horse.sex || "不明",
                sire: horse.sire || "不明",
                dam: horse.dam || "不明",
                damsire: horse.damsire || "不明",
                image_url: horse.image_url || "",
                jbis_url: horse.jbis_url || "",
                auction_url: horse.auction_url || "",
                disease_tags: Array.isArray(horse.disease_tags) ? horse.disease_tags : [],
                created_at: horse.created_at || new Date().toISOString(),
                updated_at: horse.updated_at || new Date().toISOString(),
                // オークション関連情報
                auction_date: horse.auction_date || latestAuction.auction_date || "",
                sold_price: soldPrice,
                seller: horse.seller || latestAuction.seller || "不明",
                weight: horse.weight || latestAuction.weight || 0,
                total_prize_start: horse.total_prize_start || 0,
                total_prize_latest: horse.total_prize_latest || 0,
                // 主取り（unsold）の判定
                unsold: isUnsold,
                unsold_count: auctionHistory.filter((a)=>a.sold_price === null || a.sold_price === 0).length,
                comment: horse.comment || "",
                // 計算フィールド
                roi: 0,
                price_per_kg: 0,
                display_price: "",
                display_weight: "",
                display_prize: "",
                display_roi: "",
                sort_price: 0,
                sort_prize: 0,
                sort_roi: 0,
                primary_image: imageUrlNormalized,
                detail_url: horse.auction_url || `#/horse/${horseId}`,
                auction_history: auctionHistory,
                // その他の必須フィールド
                is_auction: false,
                is_new: false,
                is_updated: false,
                is_featured: false,
                is_sold: false,
                is_unsold: isUnsold,
                is_withdrawn: false,
                is_reserved: false,
                is_favorite: false,
                is_watchlist: false,
                is_compare: false,
                is_selected: false,
                is_loading: false,
                is_error: false,
                error_message: ""
            };
            // 賞金を適切に取得（prize_moneyオブジェクトまたはtotal_prize_latestから）
            const prizeMoneyValue = (()=>{
                // 1. prize_moneyオブジェクトから取得を試みる
                if (horse.prize_money?.total_prize) {
                    const prizeStr = String(horse.prize_money.total_prize).replace(/[^0-9]/g, "");
                    const prize = parseInt(prizeStr, 10);
                    if (!isNaN(prize) && prize > 0) {
                        console.log(`[${index}] ${horse.name} - using prize_money.total_prize:`, prize);
                        return prize;
                    }
                }
                // 2. total_prize_latestから取得を試みる
                if (baseHorse.total_prize_latest) {
                    console.log(`[${index}] ${horse.name} - using total_prize_latest:`, baseHorse.total_prize_latest);
                    return baseHorse.total_prize_latest;
                }
                // 3. デフォルト値
                console.log(`[${index}] ${horse.name} - no prize money found, using 0`);
                return 0;
            })();
            // 計算フィールドを設定
            const soldPriceValue = typeof baseHorse.sold_price === "number" ? baseHorse.sold_price : 0;
            // 重量あたりの価格を計算するための重み（0除算を防ぐため1kgをデフォルト値に）
            const weightForCalc = effectiveWeight !== undefined && effectiveWeight > 0 ? effectiveWeight : 1;
            // ROI計算
            const calculatedRoiValue = soldPriceValue > 0 ? (prizeMoneyValue - soldPriceValue) / soldPriceValue * 100 : 0;
            // 重量あたりの価格を計算（0除算を防ぐ）
            const pricePerKgValue = numericSoldPrice / weightForCalc;
            // 更新された馬データを返す
            return {
                ...baseHorse,
                roi: calculatedRoiValue,
                price_per_kg: pricePerKgValue,
                display_price: getDisplayPrice({
                    unsold: isUnsold,
                    sold_price: soldPrice,
                    history: auctionHistory
                }),
                // 馬体重を数値のみで保持（表示時にkgを付与）
                weight: horseWeight,
                display_weight: horseWeight !== null ? `${horseWeight}kg` : "-",
                display_prize: (0,format/* formatPrizeFromYen */.dp)(prizeMoneyValue),
                display_roi: calculatedRoiValue > 0 ? `${calculatedRoiValue.toFixed(1)}%` : "-",
                sort_price: soldPriceValue,
                sort_prize: prizeMoneyValue,
                sort_roi: calculatedRoiValue,
                primary_image: imageUrlNormalized,
                seller: latestAuction?.seller || "",
                auction_date: latestAuction?.auction_date || "",
                comment: latestAuction?.comment || "",
                effectiveAuction: effectiveAuction
            };
        });
        // Filter out any null values and ensure we have AnalysisHorse[] type
        const filteredHorsesResult = result.filter((horse)=>horse !== null);
        console.log(`=== Transformation Summary ===`);
        console.log(`Total input horses: ${horsesArray.length}`);
        console.log(`Successfully transformed: ${filteredHorsesResult.length}`);
        console.log(`Failed to transform: ${horsesArray.length - filteredHorsesResult.length}`);
        if (filteredHorsesResult.length === 0 && horsesArray.length > 0) {
            console.error("No horses were transformed successfully. First horse data:", JSON.stringify(horsesArray[0], null, 2));
            // デバッグ用に最初の馬のデータを簡易変換して返す
            const debugHorse = {
                ...horsesArray[0],
                id: String(horsesArray[0].id || "debug-id"),
                name: horsesArray[0].name || "Unknown",
                sex: horsesArray[0].sex || "不明",
                age: horsesArray[0].age || 0,
                sire: horsesArray[0].sire || "不明",
                dam: horsesArray[0].dam || "不明",
                damsire: horsesArray[0].damsire || "不明",
                effectiveAuction: {
                    id: "debug-" + (horsesArray[0].id || "1"),
                    horse_id: String(horsesArray[0].id || "1"),
                    auction_date: horsesArray[0].auction_date || new Date().toISOString().split("T")[0],
                    sold_price: 1000,
                    total_prize_start: 0,
                    total_prize_latest: 0,
                    weight: horsesArray[0].weight || 450,
                    seller: horsesArray[0].seller || "不明",
                    is_unsold: false,
                    comment: "デバッグ用データ",
                    created_at: new Date().toISOString(),
                    detail_url: horsesArray[0].detail_url || "",
                    auction_url: horsesArray[0].auction_url || ""
                }
            };
            console.log("Debug horse data to return:", debugHorse);
            return [
                debugHorse
            ];
        }
        return filteredHorsesResult;
    } catch (error) {
        console.error("Error in transformHorseData:", error);
        return [];
    }
};
const AnalysisContent_formatPrice = (price, isUnsold = false)=>{
    // デバッグ用ログ
    console.log("formatPrice input:", {
        price,
        type: typeof price,
        isUnsold
    });
    // 主取りフラグが立っているか、価格が無効な場合は「主取り」を返す
    if (isUnsold || price === null || price === undefined || price === "") {
        console.log("formatPrice: unsold or invalid price, returning 主取り");
        return "主取り";
    }
    // 数値に変換
    let numPrice;
    if (typeof price === "string") {
        // 文字列から数値に変換（カンマや通貨記号を削除）
        numPrice = parseFloat(price.toString().replace(/[^0-9.]/g, ""));
    } else {
        numPrice = Number(price);
    }
    // 数値が無効または0以下の場合は「主取り」を返す
    if (isNaN(numPrice) || numPrice <= 0) {
        console.log("formatPrice: invalid or zero price, returning 主取り");
        return "主取り";
    }
    // 価格をフォーマット
    let formattedPrice;
    if (numPrice < 1000000) {
        // 100万円未満は「X.X万円」で表示（例: 30.5万円）
        formattedPrice = `${(numPrice / 10000).toFixed(1).replace(/\.0$/, "")}万円`;
    } else {
        // 100万円以上は「X,XXX万円」で表示（例: 1,500万円）
        formattedPrice = `${(numPrice / 10000).toLocaleString("ja-JP")}万円`;
    }
    console.log("formatPrice: formatted price:", formattedPrice);
    return formattedPrice;
};
// formatWeight, formatPrizeFromYen are imported from utils/format
const calcROI = (prize, price)=>{
    if (!prize || !price) return "-";
    // 賞金を数値に変換
    let prizeNum;
    if (typeof prize === "object" && prize !== null && "total_prize" in prize) {
        prizeNum = parseFloat(prize.total_prize.replace(/[^0-9.]/g, "")) || 0;
    } else if (typeof prize === "string") {
        prizeNum = parseFloat(prize) || 0;
    } else {
        prizeNum = prize || 0;
    }
    // 価格を数値に変換
    const numPrice = typeof price === "string" ? parseFloat(price) : price || 0;
    if (isNaN(prizeNum) || isNaN(numPrice) || numPrice === 0) return "-";
    // ROIを計算（賞金 / 価格）
    const roi = prizeNum / numPrice * 100; // パーセント表示
    return roi.toFixed(1) + "%";
};
// 数値の平均を計算する関数
const calculateAverage = (numbers)=>{
    if (!numbers.length) return 0;
    const sum = numbers.reduce((a, b)=>a + b, 0);
    return sum / numbers.length;
};
// 成長率の平均を計算する関数
const calculateAverageGrowthRate = (rates)=>{
    if (!rates.length) return 0;
    const validRates = rates.filter((rate)=>isFinite(rate) && !isNaN(rate));
    if (!validRates.length) return 0;
    const sum = validRates.reduce((a, b)=>a + b, 0);
    return sum / validRates.length;
};
// ソートアイコンをレンダリングする関数
const renderSortIcon = (key, currentSortKey, currentSortOrder)=>{
    if (currentSortKey !== key) return /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(index_esm/* FaSort */.roE, {
        className: "ml-1 opacity-30"
    });
    return currentSortOrder === "asc" ? /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(index_esm/* FaSortUp */.h3_, {
        className: "ml-1"
    }) : /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(index_esm/* FaSortDown */.sF, {
        className: "ml-1"
    });
};
function AnalysisContent() {
    const router = (0,navigation.useRouter)();
    const [data, setData] = (0,react_.useState)(null);
    const [loading, setLoading] = (0,react_.useState)(true);
    const [error, setError] = (0,react_.useState)(null);
    const [sortKey, setSortKey] = (0,react_.useState)("sort_roi");
    const [sortOrder, setSortOrder] = (0,react_.useState)("desc");
    const [showType, setShowType] = (0,react_.useState)("all");
    const [filterBySex, setFilterBySex] = (0,react_.useState)("all");
    const [filterByAge, setFilterByAge] = (0,react_.useState)("all");
    const [filterBySire, setFilterBySire] = (0,react_.useState)("all");
    const [filterByDam, setFilterByDam] = (0,react_.useState)("all");
    // データ取得関数
    const fetchData = (0,react_.useCallback)(async ()=>{
        try {
            setLoading(true);
            setError(null);
            // ローカルのJSONファイルからデータを読み込む
            const response = await fetch("/data/horses.json");
            if (!response.ok) {
                throw new Error("データの読み込みに失敗しました");
            }
            const horsesData = await response.json();
            console.log("Raw horses data:", horsesData);
            // データを変換
            const transformedHorses = transformHorseData(horsesData);
            // 統計情報を計算
            const soldPrices = transformedHorses.filter((h)=>h.sold_price !== null && h.sold_price > 0).map((h)=>h.sold_price);
            const growthRates = transformedHorses.filter((h)=>h.total_prize_start > 0 && h.total_prize_latest > h.total_prize_start).map((h)=>(h.total_prize_latest - h.total_prize_start) / h.total_prize_start * 100);
            // メタデータを生成
            const metadata = {
                last_updated: new Date().toISOString(),
                total_horses: transformedHorses.length,
                average_price: calculateAverage(soldPrices),
                average_growth_rate: calculateAverageGrowthRate(growthRates),
                horses_with_growth_data: growthRates.length
            };
            // 分析データを設定
            const analysisData = {
                horses: transformedHorses,
                metadata: metadata
            };
            console.log("Transformed analysis data:", analysisData);
            setData(analysisData);
        } catch (error) {
            console.error("データの取得中にエラーが発生しました:", error);
            setError("データの取得中にエラーが発生しました");
        } finally{
            setLoading(false);
        }
    }, []);
    (0,react_.useEffect)(()=>{
        fetchData();
    }, [
        fetchData
    ]);
    // ソート関数の定義
    const sortFunctions = (0,react_.useMemo)(()=>({
            sort_price: (a, b)=>(a.sort_price || 0) - (b.sort_price || 0),
            sort_prize: (a, b)=>(a.sort_prize || 0) - (b.sort_prize || 0),
            sort_roi: (a, b)=>(a.sort_roi || 0) - (b.sort_roi || 0),
            sex: (a, b)=>(a.sex || "").localeCompare(b.sex || "", "ja"),
            age: (a, b)=>(a.age || 0) - (b.age || 0),
            sire: (a, b)=>(a.sire || "").localeCompare(b.sire || "", "ja"),
            dam: (a, b)=>(a.dam || "").localeCompare(b.dam || "", "ja"),
            unsold: (a, b)=>(a.unsold ? 1 : 0) - (b.unsold ? 1 : 0)
        }), []);
    // ソート処理
    const handleSort = (0,react_.useCallback)((key)=>{
        if (sortKey === key) {
            setSortOrder(sortOrder === "asc" ? "desc" : "asc");
        } else {
            setSortKey(key);
            setSortOrder("desc");
        }
    }, [
        sortKey,
        sortOrder
    ]);
    // ローディング中の表示
    if (loading) {
        return /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("div", {
            className: "min-h-screen flex items-center justify-center",
            children: "Loading..."
        });
    }
    // エラーまたはデータがない場合の表示
    if (error || !data) {
        return /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("div", {
            className: "min-h-screen flex items-center justify-center text-red-600",
            children: error || "データがありません"
        });
    }
    // データを変換
    console.log("元の馬データ:", data.horses);
    // 表示用の馬データを準備
    let tableHorses = data.horses.map((horse)=>{
        // オークション履歴を取得（auction_historyがなければ空配列を使用）
        const auctionHistory = Array.isArray(horse.auction_history) ? horse.auction_history : [];
        // 最新のオークション情報を取得（なければデフォルト値を使用）
        const latestAuction = auctionHistory[0] || {
            auction_date: "",
            sold_price: 0,
            is_unsold: true,
            seller: "",
            weight: 0,
            total_prize_start: 0,
            total_prize_latest: 0,
            detail_url: ""
        };
        // 有効な体重を計算
        const effectiveWeight = (()=>{
            // 馬オブジェクトの体重を取得
            const horseWeight = Number(horse.weight) || 0;
            // オークション履歴の体重を取得
            const auctionWeight = latestAuction ? Number(latestAuction.weight) || 0 : 0;
            // 有効な体重を返す（0より大きい値のみ有効）
            return horseWeight > 0 ? Math.floor(horseWeight) : auctionWeight > 0 ? Math.floor(auctionWeight) : undefined;
        })();
        return {
            ...horse,
            ...latestAuction,
            effectiveWeight,
            unsold_count: auctionHistory.filter((a)=>a.is_unsold).length,
            detail_url: latestAuction.detail_url || latestAuction.auction_url || horse.detail_url || horse.auction_url || "",
            total_prize_start: latestAuction.total_prize_start,
            total_prize_latest: latestAuction.total_prize_latest || 0,
            sold_price: latestAuction.sold_price || 0,
            unsold: latestAuction.is_unsold || (horse.unsold_count ? horse.unsold_count > 0 : false)
        };
    });
    console.log("変換後の馬データ:", tableHorses);
    // 表示タイプによるフィルタリング
    if (showType === "sold") {
        tableHorses = tableHorses.filter((horse)=>!horse.unsold);
    } else if (showType === "unsold") {
        tableHorses = tableHorses.filter((horse)=>horse.unsold);
    } else if (showType === "roi") {
        tableHorses = tableHorses.filter((horse)=>horse.roi > 0);
    } else if (showType === "value") {
        const avgPrice = data.metadata.average_price;
        tableHorses = tableHorses.filter((horse)=>!horse.unsold && horse.sold_price && horse.sold_price > 0 && horse.sold_price < avgPrice && horse.roi > 0);
    }
    // 性別によるフィルタリング
    if (filterBySex !== "all") {
        tableHorses = tableHorses.filter((horse)=>horse.sex === filterBySex);
    }
    // 年齢によるフィルタリング
    if (filterByAge !== "all") {
        tableHorses = tableHorses.filter((horse)=>String(horse.age) === filterByAge);
    }
    // 父馬によるフィルタリング
    if (filterBySire !== "all") {
        tableHorses = tableHorses.filter((horse)=>horse.sire === filterBySire);
    }
    // 母馬によるフィルタリング
    if (filterByDam !== "all") {
        tableHorses = tableHorses.filter((horse)=>horse.dam === filterByDam);
    }
    // ソート処理の適用
    console.log("ソート前の馬データ:", tableHorses);
    if (sortKey && sortFunctions[sortKey]) {
        // 主取り馬のフィルタリングを調整
        let filteredHorses = [
            ...tableHorses
        ];
        // 主取り馬を除外する場合は以下のコメントアウトを外す
        // filteredHorses = filteredHorses.filter((h: AnalysisHorse) => !h.unsold_count || h.unsold_count === 0);
        tableHorses = filteredHorses.sort((a, b)=>{
            const sortFn = sortFunctions[sortKey];
            const result = sortFn(a, b);
            return sortOrder === "asc" ? result : -result;
        });
    }
    const horsesWithLatest = data.horses.map((horse)=>{
        // オークション履歴を取得（auction_historyがなければ空配列を使用）
        const auctionHistory = Array.isArray(horse.auction_history) ? horse.auction_history : [];
        // 馬体重を取得するヘルパー関数
        const getHorseWeight = ()=>{
            if (horse.weight !== undefined && horse.weight !== null && Number(horse.weight) > 0) {
                return Number(horse.weight);
            }
            // オークション履歴から最新の体重を確認
            if (auctionHistory.length > 0) {
                const validAuction = auctionHistory.find((a)=>a.weight !== undefined && a.weight !== null && Number(a.weight) > 0);
                if (validAuction) {
                    return Number(validAuction.weight);
                }
            }
            return null;
        };
        const currentHorseWeight = getHorseWeight();
        // 最新のオークション履歴を取得（存在しない場合はデフォルト値を持つオブジェクトを返す）
        const latestAuction = auctionHistory.length > 0 ? {
            ...auctionHistory[0],
            weight: auctionHistory[0].weight || currentHorseWeight
        } : {
            id: "",
            horse_id: horse.id,
            auction_date: "",
            sold_price: null,
            total_prize_start: 0,
            total_prize_latest: 0,
            weight: currentHorseWeight,
            seller: "",
            is_unsold: false,
            comment: "",
            created_at: new Date().toISOString(),
            detail_url: "",
            auction_url: ""
        };
        // 主取り判定
        const isUnsold = horse.is_unsold === true || horse.unsold === true || latestAuction && latestAuction.is_unsold === true || latestAuction && latestAuction.unsold === true || latestAuction && latestAuction.sold_price === 0 || horse.sold_price === 0;
        // 馬の基本情報と最新のオークション情報をマージ
        const horseWithDetails = {
            ...horse,
            ...latestAuction,
            unsold_count: auctionHistory.filter((a)=>a.is_unsold).length,
            detail_url: latestAuction.detail_url || latestAuction.auction_url || horse.detail_url || horse.auction_url || "",
            total_prize_start: latestAuction.total_prize_start,
            total_prize_latest: latestAuction.total_prize_latest || 0,
            weight: latestAuction.weight,
            display_weight: latestAuction.weight !== null ? `${latestAuction.weight}kg` : "-",
            sold_price: latestAuction.sold_price !== null ? latestAuction.sold_price : horse.sold_price || 0,
            unsold: isUnsold,
            is_unsold: isUnsold
        };
        return horseWithDetails;
    });
    // 平均ROIの計算
    const avgROI = tableHorses.length > 0 ? tableHorses.reduce((sum, h)=>{
        const soldPrice = h.sold_price || 0;
        const prizeMoney = h.total_prize_latest || 0;
        const roi = soldPrice > 0 ? prizeMoney / soldPrice : 0;
        return sum + roi;
    }, 0) / tableHorses.length : 0;
    // ROIランキングの計算
    const roiRanking = tableHorses.filter((h)=>{
        const soldPrice = h.sold_price || 0;
        const prizeMoney = h.total_prize_latest || 0;
        return soldPrice > 0 && prizeMoney > 0;
    }).sort((a, b)=>{
        const aROI = (a.total_prize_latest || 0) / (a.sold_price || 1);
        const bROI = (b.total_prize_latest || 0) / (b.sold_price || 1);
        return bROI - aROI;
    }).slice(0, 10);
    // 価値のある馬のフィルタリング
    const valueHorses = tableHorses.filter((h)=>{
        const soldPrice = h.sold_price || 0;
        const prizeMoney = h.total_prize_latest || 0;
        const roi = soldPrice > 0 ? prizeMoney / soldPrice : 0;
        return soldPrice > 0 && roi > avgROI && soldPrice < data.metadata.average_price;
    });
    // 年齢を表示するヘルパー関数（スクレイピングデータをそのまま表示）
    const displayAge = (age)=>{
        if (age === null || age === undefined || age === "") return "-";
        return `${age}歳`;
    };
    const displayPrice = (price, horse)=>{
        // 明示的に主取りフラグが立っているときは「主取り」を返す
        const isUnsold = horse.is_unsold === true || horse.unsold === true || horse.effectiveAuction && (horse.effectiveAuction.is_unsold === true || horse.effectiveAuction.unsold === true);
        if (isUnsold) {
            return "主取り";
        }
        // 価格がnullまたはundefinedの場合は「-」を返す
        if (price === null || price === undefined) {
            return "-";
        }
        // 価格が0の場合は0円と表示
        if (price === 0 || price === "0") {
            return AnalysisContent_formatPrice(0);
        }
        // それ以外の場合はフォーマットして返す
        return AnalysisContent_formatPrice(price);
    };
    // ソート処理の適用
    console.log("フィルタリング後の馬データ:", tableHorses);
    if (sortKey && sortFunctions[sortKey]) {
        tableHorses = [
            ...tableHorses
        ].sort((a, b)=>{
            const result = sortFunctions[sortKey](a, b);
            return sortOrder === "asc" ? result : -result;
        });
        console.log("ソート後の馬データ:", tableHorses);
    }
    // Helper function to safely get detail URL
    const getDetailUrl = (horse)=>{
        return horse.detail_url || "";
    };
    console.log("レンダリング時の馬データ:", tableHorses);
    return /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("div", {
        className: "min-h-screen bg-gray-50 px-4 py-8",
        children: /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
            className: "max-w-7xl mx-auto",
            children: [
                /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                    className: "mb-4 p-4 bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700",
                    children: [
                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("p", {
                            children: [
                                "デバッグ情報: 表示中の馬の数: ",
                                tableHorses.length
                            ]
                        }),
                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("p", {
                            children: [
                                "表示タイプ: ",
                                showType
                            ]
                        }),
                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("p", {
                            children: [
                                "フィルター: 性別=",
                                filterBySex,
                                ", 年齢=",
                                filterByAge,
                                ", 父馬=",
                                filterBySire,
                                ", 母馬=",
                                filterByDam
                            ]
                        })
                    ]
                }),
                /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                    className: "mb-6 text-lg font-semibold text-gray-700 flex flex-wrap gap-8",
                    children: [
                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("span", {
                            children: [
                                "総馬数: ",
                                tableHorses.length
                            ]
                        }),
                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("span", {
                            children: [
                                "平均落札価格: ",
                                AnalysisContent_formatPrice(data.metadata.average_price)
                            ]
                        }),
                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("span", {
                            children: [
                                "平均ROI: ",
                                avgROI.toFixed(2)
                            ]
                        })
                    ]
                }),
                /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                    className: "flex gap-4 mb-6",
                    children: [
                        /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(ui_button/* Button */.z, {
                            onClick: ()=>setShowType("all"),
                            variant: "default",
                            className: showType === "all" ? "bg-blue-600 text-white" : "bg-blue-400 text-white",
                            children: "全馬"
                        }),
                        /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(ui_button/* Button */.z, {
                            onClick: ()=>setShowType("roi"),
                            variant: "default",
                            className: showType === "roi" ? "bg-green-600 text-white" : "bg-green-400 text-white",
                            children: "ROIランキング"
                        }),
                        /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(ui_button/* Button */.z, {
                            onClick: ()=>setShowType("value"),
                            variant: "default",
                            className: showType === "value" ? "bg-orange-600 text-white" : "bg-orange-400 text-white",
                            children: "妙味馬"
                        })
                    ]
                }),
                /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("div", {
                    className: "overflow-x-auto bg-white rounded-lg shadow",
                    children: /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("table", {
                        className: "min-w-full divide-y divide-gray-200",
                        children: [
                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("thead", {
                                className: "bg-gray-100",
                                children: /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("tr", {
                                    children: [
                                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("th", {
                                            className: "px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase cursor-pointer",
                                            onClick: ()=>handleSort("name"),
                                            children: [
                                                "馬名",
                                                renderSortIcon("name", sortKey, sortOrder)
                                            ]
                                        }),
                                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("th", {
                                            className: "px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase cursor-pointer",
                                            onClick: ()=>handleSort("sex"),
                                            children: [
                                                "性別",
                                                renderSortIcon("sex", sortKey, sortOrder)
                                            ]
                                        }),
                                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("th", {
                                            className: "px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase cursor-pointer",
                                            onClick: ()=>handleSort("age"),
                                            children: [
                                                "年齢",
                                                renderSortIcon("age", sortKey, sortOrder)
                                            ]
                                        }),
                                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("th", {
                                            className: "px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase cursor-pointer",
                                            onClick: ()=>handleSort("sire"),
                                            children: [
                                                "父",
                                                renderSortIcon("sire", sortKey, sortOrder)
                                            ]
                                        }),
                                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("th", {
                                            className: "px-3 py-2 text-right text-xs font-medium text-gray-500 uppercase cursor-pointer",
                                            onClick: ()=>handleSort("weight"),
                                            children: [
                                                "馬体重 (kg)",
                                                renderSortIcon("weight", sortKey, sortOrder)
                                            ]
                                        }),
                                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("th", {
                                            className: "px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase cursor-pointer",
                                            onClick: ()=>handleSort("sold_price"),
                                            children: [
                                                "落札価格",
                                                renderSortIcon("sold_price", sortKey, sortOrder)
                                            ]
                                        }),
                                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("th", {
                                            className: "px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase cursor-pointer",
                                            onClick: ()=>handleSort("total_prize_start"),
                                            children: [
                                                "オークション時賞金",
                                                renderSortIcon("total_prize_start", sortKey, sortOrder)
                                            ]
                                        }),
                                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("th", {
                                            className: "px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase cursor-pointer",
                                            onClick: ()=>handleSort("total_prize_latest"),
                                            children: [
                                                "現在賞金",
                                                renderSortIcon("total_prize_latest", sortKey, sortOrder)
                                            ]
                                        }),
                                        /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("th", {
                                            className: "px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase cursor-pointer",
                                            onClick: ()=>handleSort("roi"),
                                            children: [
                                                "ROI",
                                                renderSortIcon("roi", sortKey, sortOrder)
                                            ]
                                        }),
                                        /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("th", {
                                            className: "px-2 py-2 text-center text-xs font-medium text-gray-500 uppercase",
                                            children: "リンク"
                                        }),
                                        /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("th", {
                                            className: "px-2 py-2 text-center text-xs font-medium text-gray-500 uppercase w-24",
                                            children: "病歴"
                                        })
                                    ]
                                })
                            }),
                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("tbody", {
                                className: "divide-y divide-gray-200",
                                children: tableHorses.map((horse, index)=>/*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("tr", {
                                        className: "hover:bg-blue-50",
                                        children: [
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2 font-medium text-gray-900",
                                                children: horse.id ? /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ((link_default()), {
                                                    href: `/horses/${horse.id}`,
                                                    className: "hover:underline text-blue-700",
                                                    onClick: (e)=>{
                                                        console.log("Navigating to horse:", horse.id, "Name:", horse.name);
                                                    },
                                                    children: horse.name
                                                }) : /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("span", {
                                                    children: horse.name
                                                })
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2",
                                                children: horse.sex
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2",
                                                children: displayAge(horse.age)
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2",
                                                children: horse.sire
                                            }),
                                            /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("td", {
                                                className: "px-3 py-2 text-right",
                                                children: [
                                                    horse.effectiveWeight !== undefined ? `${horse.effectiveWeight}kg` : "-",
                                                     false && /*#__PURE__*/ 0
                                                ]
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2",
                                                children: horse.display_price
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2",
                                                children: "-"
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2",
                                                children: horse.prize_money ? (0,format/* formatPrizeFromYen */.dp)(horse.prize_money.total_prize) : "-"
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2",
                                                children: horse.prize_money ? calcROI(horse.prize_money.total_prize, horse.sold_price ?? undefined) : calcROI(undefined, horse.sold_price ?? undefined)
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2",
                                                children: /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                                                    className: "flex flex-col gap-1 items-center",
                                                    children: [
                                                        horse.jbis_url && /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("a", {
                                                            href: horse.jbis_url,
                                                            target: "_blank",
                                                            rel: "noopener noreferrer",
                                                            className: "text-xs text-blue-600 hover:text-blue-800 underline whitespace-nowrap",
                                                            title: "JBISで詳細を確認",
                                                            children: "JBIS"
                                                        }),
                                                        horse.auction_url && /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("a", {
                                                            href: horse.auction_url,
                                                            target: "_blank",
                                                            rel: "noopener noreferrer",
                                                            className: "text-xs text-green-600 hover:text-green-800 underline whitespace-nowrap",
                                                            title: "オークションページで詳細を確認",
                                                            children: "サラオク"
                                                        })
                                                    ]
                                                })
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("td", {
                                                className: "px-3 py-2 text-center",
                                                children: (()=>{
                                                    // 病歴が「なし」の馬を判定
                                                    const isNoDisease = (tags)=>{
                                                        if (tags === undefined || tags === null || tags === "") return true;
                                                        if (Array.isArray(tags)) {
                                                            if (tags.length === 0) return true;
                                                            return tags.every((tag)=>{
                                                                const strTag = String(tag).trim();
                                                                return strTag === "" || strTag === "-" || strTag === "なし" || strTag === "なし。" || strTag === "特になし" || strTag === "特になし。";
                                                            });
                                                        }
                                                        const strTag = String(tags).trim();
                                                        return strTag === "" || strTag === "-" || strTag === "なし" || strTag === "なし。" || strTag === "特になし" || strTag === "特になし。";
                                                    };
                                                    // 病歴が「なし」の場合は青で表示、それ以外はピンクで「あり」と表示
                                                    return isNoDisease(horse.disease_tags) ? /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("span", {
                                                        className: "text-xs font-medium bg-blue-50 text-blue-600 px-2 py-0.5 rounded-full whitespace-nowrap inline-block w-12",
                                                        children: "なし"
                                                    }) : /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("span", {
                                                        className: "text-xs font-medium bg-pink-100 text-pink-800 px-2 py-0.5 rounded-full whitespace-nowrap inline-block w-12",
                                                        children: "あり"
                                                    });
                                                })()
                                            })
                                        ]
                                    }, `${horse.id || "horse"}-${index}`))
                            })
                        ]
                    })
                })
            ]
        })
    });
}

// EXTERNAL MODULE: ./src/lib/utils.ts
var utils = __webpack_require__(12019);
;// CONCATENATED MODULE: ./src/components/ui/alert.tsx



const Alert = /*#__PURE__*/ react_.forwardRef(({ className, variant = "default", children, ...props }, ref)=>{
    return /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("div", {
        ref: ref,
        role: "alert",
        className: (0,utils.cn)("relative w-full rounded-lg border p-4 [&>svg~*]:pl-7 [&>svg+div]:translate-y-[-3px] [&>svg]:absolute [&>svg]:left-4 [&>svg]:top-4 [&>svg]:text-foreground", {
            "bg-background text-foreground": variant === "default",
            "border-destructive/50 text-destructive [&>svg]:text-destructive dark:border-destructive [&>svg]:text-destructive": variant === "destructive"
        }, className),
        ...props,
        children: children
    });
});
Alert.displayName = "Alert";
const AlertTitle = /*#__PURE__*/ react_.forwardRef(({ className, ...props }, ref)=>/*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("h5", {
        ref: ref,
        className: (0,utils.cn)("mb-1 font-medium leading-none tracking-tight", className),
        ...props
    }));
AlertTitle.displayName = "AlertTitle";
const AlertDescription = /*#__PURE__*/ react_.forwardRef(({ className, ...props }, ref)=>/*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("div", {
        ref: ref,
        className: (0,utils.cn)("text-sm [&_p]:leading-relaxed", className),
        ...props
    }));
AlertDescription.displayName = "AlertDescription";


// EXTERNAL MODULE: ./node_modules/lucide-react/dist/esm/icons/alert-circle.mjs
var alert_circle = __webpack_require__(3285);
// EXTERNAL MODULE: ./node_modules/lucide-react/dist/esm/icons/x.mjs
var x = __webpack_require__(95833);
;// CONCATENATED MODULE: ./src/components/DataIntegrityAlert.tsx






function DataIntegrityAlert() {
    const [isOpen, setIsOpen] = react_.useState(true);
    // アラートを非表示にする
    if (!isOpen) {
        return null;
    }
    return /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)(Alert, {
        variant: "default",
        className: "mb-4",
        children: [
            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(alert_circle/* default */.Z, {
                className: "h-4 w-4"
            }),
            /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                className: "flex justify-between items-start",
                children: [
                    /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                        children: [
                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(AlertTitle, {
                                children: "データの整合性チェック"
                            }),
                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(AlertDescription, {
                                className: "mt-2",
                                children: "データの整合性を確認中です。問題がある場合はここに表示されます。"
                            })
                        ]
                    }),
                    /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)(ui_button/* Button */.z, {
                        variant: "ghost",
                        size: "icon",
                        className: "h-6 w-6 -mt-1 -mr-2",
                        onClick: ()=>setIsOpen(false),
                        children: [
                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(x/* default */.Z, {
                                className: "h-4 w-4"
                            }),
                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("span", {
                                className: "sr-only",
                                children: "閉じる"
                            })
                        ]
                    })
                ]
            })
        ]
    });
}

;// CONCATENATED MODULE: ./app/page.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 





function Home() {
    const router = (0,navigation.useRouter)();
    // 最新の年齢を取得するヘルパー関数
    const getLatestAge = (horse)=>{
        // トップレベルの年齢を返す
        return horse.age?.toString() || "";
    };
    // ヘッダー部分を残して、コンテンツ部分をAnalysisContentに置き換え
    return /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)(emotion_react_jsx_runtime_esm/* Fragment */.HY, {
        children: [
            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(DataIntegrityAlert, {}),
            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("header", {
                className: "bg-white shadow-sm border-b",
                children: /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("div", {
                    className: "max-w-7xl mx-auto px-4 sm:px-6 lg:px-8",
                    children: /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                        className: "flex justify-between items-center py-4",
                        children: [
                            /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                                className: "flex items-center gap-4",
                                children: [
                                    /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("h1", {
                                        className: "text-2xl font-bold text-gray-900",
                                        children: "サラオクDB"
                                    }),
                                    /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                                        className: "hidden md:flex items-center text-sm text-gray-600 border-l border-gray-300 pl-4",
                                        children: [
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("span", {
                                                children: "次回更新: "
                                            }),
                                            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ("span", {
                                                className: "font-medium ml-1",
                                                children: "毎週木・日 24:00"
                                            })
                                        ]
                                    })
                                ]
                            }),
                            /*#__PURE__*/ (0,emotion_react_jsx_runtime_esm/* jsxs */.BX)("div", {
                                className: "flex gap-4",
                                children: [
                                    /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ((link_default()), {
                                        href: "/#",
                                        children: /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(ui_button/* Button */.z, {
                                            variant: "outline",
                                            className: "rounded-md bg-white border border-black text-black hover:bg-gray-100",
                                            children: "解析"
                                        })
                                    }),
                                    /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ((link_default()), {
                                        href: "/horses",
                                        children: /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(ui_button/* Button */.z, {
                                            variant: "outline",
                                            className: "rounded-md bg-white border border-black text-black hover:bg-gray-100",
                                            children: "直近の追加"
                                        })
                                    })
                                ]
                            })
                        ]
                    })
                })
            }),
            /*#__PURE__*/ emotion_react_jsx_runtime_esm/* jsx */.tZ(AnalysisContent, {})
        ]
    });
}


/***/ }),

/***/ 92838:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   $$typeof: () => (/* binding */ $$typeof),
/* harmony export */   __esModule: () => (/* binding */ __esModule),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var next_dist_build_webpack_loaders_next_flight_loader_module_proxy__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(61363);

const proxy = (0,next_dist_build_webpack_loaders_next_flight_loader_module_proxy__WEBPACK_IMPORTED_MODULE_0__.createProxy)(String.raw`/Users/yum.ishii/SaraokuDB/frontend/app/page.tsx`)

// Accessing the __esModule property and exporting $$typeof are required here.
// The __esModule getter forces the proxy target to create the default export
// and the $$typeof value is for rendering logic to determine if the module
// is a client boundary.
const { __esModule, $$typeof } = proxy;
const __default__ = proxy.default;


/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__default__);

/***/ })

};
;

// load runtime
var __webpack_require__ = require("../webpack-runtime.js");
__webpack_require__.C(exports);
var __webpack_exec__ = (moduleId) => (__webpack_require__(__webpack_require__.s = moduleId))
var __webpack_exports__ = __webpack_require__.X(0, [587,668,489,440,420,368,610,256,191], () => (__webpack_exec__(56274)));
module.exports = __webpack_exports__;

})();