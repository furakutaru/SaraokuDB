# サラオクDB プロジェクト仕様書

最終更新日: 2025-08-27

## 1. プロジェクト概要

サラオクDBは、楽天サラブレッドオークションのデータを収集・分析するためのシステムです。馬の血統情報、落札価格、賞金情報などを一元管理し、投資判断を支援します。

## 2. システムアーキテクチャ

### 2.1 全体構成

```
フロントエンド (Next.js)  ←→  バックエンド (Python)  ←→  データソース
    │                               │
    ▼                               ▼
ブラウザ表示               スクレイピング・データ処理
```

### 2.2 技術スタック

- **フロントエンド**
  - Next.js 13.4.12
  - TypeScript
  - Tailwind CSS

- **バックエンド**
  - Python 3.11+
  - BeautifulSoup4
  - Requests
  - SQLite (データストア)

- **開発ツール**
  - Git
  - npm / yarn
  - pip
  - VSCode 推奨構成

## 3. ディレクトリ構成

```
.
├── backend/                    # バックエンド関連
│   ├── database/              # データベース関連スクリプト
│   ├── scrapers/              # スクレイピングスクリプト
│   └── utils/                 # ユーティリティ関数
│
├── static-frontend/           # フロントエンドアプリケーション
│   ├── public/                # 静的ファイル
│   └── src/                   # ソースコード
│       ├── components/        # Reactコンポーネント
│       ├── pages/             # ページコンポーネント
│       └── styles/            # スタイルシート
│
├── scripts/                   # スクリプトとユーティリティ
│   ├── components/            # スクレイピングコンポーネント
│   └── core/                  # コア機能
│
├── data/                      # データファイル
│   ├── horses.json           # 馬マスターデータ
│   └── auction_history.json  # オークション履歴データ
│
└── docs/                      # ドキュメント
    └── SCRAPING_GUIDE.md     # スクレイピングガイド
```

## 4. 自動化スケジュール

### 4.1 定期的なスクレイピング

- **週2回のオークション情報収集**
  - 毎週火曜日・金曜日 20:00 JST に実行
  - 新規馬情報の取得と既存情報の更新

- **日次バックアップ**
  - 毎日 04:00 JST にデータベースのバックアップを実行
  - 過去7日分のバックアップを保持

### 4.2 メンテナンスタスク

- **月次メンテナンス**
  - ログファイルのローテーション
  - 一時ファイルのクリーンアップ
  - データベースの最適化

## 5. データ構造

### 5.1 馬マスターテーブル (horses.json)

```json
{
  "id": "string",                 // 馬の一意識別子（UUID）
  "name": "string",              // 馬名
  "sire": "string",              // 父馬名
  "dam": "string",               // 母馬名
  "damsire": "string",           // 母父名
  "sex": "string",               // 性別（牡・牝・セ）
  "age": number,                 // 年齢
  "image_url": "string",         // 馬の画像URL
  "jbis_url": "string",          // JBISの詳細ページURL
  "auction_url": "string",       // サラブレッドオークションの詳細ページURL
  "disease_tags": ["string"],    // 疾病情報のタグ配列
  "created_at": "string",        // 初回登録日時（ISO 8601形式）
  "updated_at": "string"         // 最終更新日時（ISO 8601形式）
}
```

### 5.2 オークション履歴テーブル (auction_history.json)

```json
{
  "id": "string",                // オークション履歴の一意識別子（UUID）
  "horse_id": "string",          // 馬ID（horses.idへの外部キー）
  "auction_date": "string",      // オークション日（YYYY-MM-DD形式）
  "sold_price": number | null,   // 落札価格（未落札の場合はnull）
  "total_prize_start": number,   // オークション時点の総賞金（万円）
  "total_prize_latest": number,  // 最新の総賞金（万円）
  "weight": number | null,       // 馬体重（kg、計測されていない場合はnull）
  "seller": "string",           // 売り主名
  "is_unsold": boolean,          // 主取りフラグ
  "comment": "string",          // コメント
  "created_at": "string"        // 登録日時（ISO 8601形式）
}
```

## 6. フロントエンド仕様

### 6.1 画面構成

1. **トップページ**
   - 馬一覧表示
   - 検索・フィルタリング機能
   - ソート機能

2. **馬詳細ページ**
   - 基本情報（血統、性別、年齢など）
   - オークション履歴
   - 賞金情報
   - RIO（投資収益率）計算

3. **分析ページ**
   - 平均落札価格
   - 賞金推移
   - 血統別分析

### 6.2 主な機能

- **RIO（Return on Investment）計算**
  - 計算式: `RIO = (落札後に稼いだ賞金総額) ÷ 落札価格`
  - 落札後に稼いだ賞金総額 = 現在の総賞金 - オークション時の総賞金
  - 表示: パーセンテージ（例: 15.0%）

- **検索・フィルタリング**
  - 馬名検索
  - 血統検索
  - 価格帯フィルタ
  - 賞金額フィルタ

- **ソート機能**
  - 落札価格（昇順/降順）
  - 賞金（昇順/降順）
  - RIO（昇順/降順）
  - オークション日（昇順/降順）

## 7. 技術仕様

### 7.1 スクレイピング仕様

- **対象サイト**: 楽天サラブレッドオークション
- **スクレイピング間隔**: 30秒以上の間隔を空ける
- **リトライ回数**: 最大3回
- **エラーハンドリング**: エラー時はログに記録し、処理を継続

### 7.2 データ更新フロー

1. オークション一覧ページから馬のリストを取得
2. 各馬の詳細ページをスクレイピング
3. JBISから賞金情報を取得
4. データを整形して保存
5. フロントエンドに反映

### 7.3 パフォーマンス要件

- ページロード時間: 3秒以内
- 同時接続数: 100リクエスト/秒
- データ更新間隔: 1時間ごと（手動更新も可能）

## 8. 開発環境構築

### 8.1 環境構築手順

1. リポジトリをクローン
   ```bash
   git clone https://github.com/yourusername/saraokudb.git
   cd saraokudb
   ```

2. フロントエンドのセットアップ
   ```bash
   cd static-frontend
   npm install
   ```

3. バックエンドのセットアップ
   ```bash
   pip install -r requirements.txt
   ```

4. 環境変数の設定
   ```bash
   cp .env.example .env
   # .envファイルを編集
   ```

5. 開発サーバーの起動
   ```bash
   # フロントエンド
   cd static-frontend
   npm run dev
   
   # 別ターミナルでバックエンド
   python scripts/improved_scraper.py
   ```

### 8.2 テストの実行

```bash
# フロントエンドのテスト
cd static-frontend
npm test

# バックエンドのテスト
pytest
```

## 9. デプロイ

### 9.1 本番環境

- **ホスティング**: Vercel
- **デプロイ方法**: GitHubリポジトリと連携し、mainブランチへのプッシュで自動デプロイ
- **環境変数**: Vercelのダッシュボードで設定

### 9.2 デプロイ手順

1. コードをmainブランチにプッシュ
   ```bash
   git push origin main
   ```

2. Vercelが自動的にビルドとデプロイを実行

3. デプロイの確認
   - Vercelダッシュボードでビルドステータスを確認
   - 本番URLにアクセスして動作確認・運用

## 10. トラブルシューティング

### 10.1 よくある問題と解決策

#### スクレイピングが失敗する
- **原因**: サイトのHTML構造が変更された
- **解決策**:
  1. エラーログを確認
  2. セレクタを更新
  3. テストを実行して確認

#### データが更新されない
- **原因**: スクリプトが実行されていない
- **解決策**:
  1. スケジューラの状態を確認
  2. 手動でスクリプトを実行
  3. ログを確認してエラーを特定

### 10.2 ログの確認方法

```bash
# バックエンドのログ
cat logs/backend.log

# フロントエンドのログ
cd static-frontend
npm run logs
```

## 11. 開発ガイドライン

### 11.1 コーディング規約

- **フロントエンド**
  - TypeScriptの型定義を厳格に使用
  - コンポーネントは関数コンポーネントで作成
  - スタイリングにはTailwind CSSを使用

- **バックエンド**
  - PEP 8に準拠
  - 型ヒントを使用
  - ドキュメント文字列を記述

### 11.2 ブランチ戦略

- `main`: 本番環境用ブランチ
- `develop`: 開発用ブランチ
- `feature/*`: 機能追加用ブランチ
- `fix/*`: バグ修正用ブランチ

### 11.3 プルリクエストの手順

1. 機能ブランチを作成
   ```bash
   git checkout -b feature/your-feature
   ```

2. 変更をコミット
   ```bash
   git add .
   git commit -m "Add: 新機能を追加"
   ```

3. リモートにプッシュ
   ```bash
   git push origin feature/your-feature
   ```

4. GitHubでプルリクエストを作成
   - `develop`ブランチをターゲットに設定
   - 変更内容を詳細に記述
   - 関連するIssueがあればリンク

## 12. 今後の課題

### 12.1 短期的な課題

- [ ] パフォーマンスの最適化
- [ ] テストカバレッジの向上
- [ ] ドキュメントの充実

### 12.2 中長期的な課題

- [ ] 機械学習を活用した予測機能の追加
- [ ] モバイルアプリの開発
- [ ] 他オークションサイトとの連携

### 12.3 バグ修正

- [ ] データの不整合の修正
- [ ] スクレイピングの安定性向上
- [ ] エラーハンドリングの強化

### 12.4 機能追加

- [ ] 血統分析機能
- [ ] 価格予測機能
- [ ] データエクスポート機能

### 技術改善候補
- データベース移行（PostgreSQL）
- リアルタイム更新
- PWA対応
- オフライン対応

---

**最終更新**: 2025-07-20
**バージョン**: 1.0.0
**管理者**: サラオクDB開発チーム 