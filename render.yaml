services:
  - type: web
    name: saraoku-db-api
    env: python
    plan: free
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "python backend/main.py"
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false
